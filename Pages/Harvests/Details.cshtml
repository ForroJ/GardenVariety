@page
        @model GardenVariety.Pages.Harvests.DetailsModel
        <head>
           <link rel = "stylesheet" href="~/css/site.css" asp-append-version="true" />
           <link rel = "stylesheet" href="~/GardenVariety.styles.css" asp-append-version="true" />
        </head>
        @{
           ViewData["Title"] = "Details";
        }

        <h1>Details</h1>

        <h2>Harvest Details</h2>

        <div>  
           <dl>
              <dt>Date</dt>
              <dd class="col-sm-10">@Model.Harvester.Date.ToString("MM/dd/yyyy")</dd>
              <dt>Garden Name</dt>
              <dd class="col-sm-10">@Model.Harvester.Garden.Name</dd>
              
           </dl>
        </div>

        <a asp-page="./Index">Back to List</a>
