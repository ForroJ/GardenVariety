@page  
@model GardenVariety.Pages.Harvests.EditModel  
@{  
   ViewData["Title"] = "Edit";  
}  

<h1>Edit</h1>

<h4>Harvest</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Harvester.Id"/>
            <div class="form-group">
        <label asp-for="Harvester.Date" class="control-label"></label>
        <input asp-for="Harvester.Date" type="date" class="form-control" />
        <span asp-validation-for="Harvester.Date" class="text-danger"></span>  
   </div>
   <div class="form-group">
                <label asp-for="Harvester.GardenId" class="control-label"></label>
                <select asp-for="Harvester.GardenId" class="form-control" asp-items="@(ViewData["GardenId"] as IEnumerable<SelectListItem>)"></select>
                <span asp-validation-for="Harvester.GardenId" class="text-danger"></span>
            </div>
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}